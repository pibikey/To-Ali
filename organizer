<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>For My Love 💕</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  <style>
    /* Reset and Basic Styles */
    html, body {
      margin: 0;
      padding: 0;
      height: 100%;
      font-family: 'Poppins', Arial, sans-serif;
      overflow-x: hidden;
      scroll-behavior: smooth;
      background: linear-gradient(135deg, #ff9ec1, #ff5fa2);
      animation: gradientBG 15s ease infinite;
      color: white;
      text-align: center;
    }

    @keyframes gradientBG {
      0% {background-position: 0% 50%;}
      50% {background-position: 100% 50%;}
      100% {background-position: 0% 50%;}
    }

    h1 {
      font-size: 3em;
      margin: 20px 0;
      text-shadow: 2px 2px 15px rgba(0,0,0,0.3);
    }

    /* Tabs */
    .tabs {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 15px;
      margin-bottom: 30px;
    }

    .tab-btn {
      padding: 10px 20px;
      font-size: 1em;
      border-radius: 25px;
      border: none;
      cursor: pointer;
      background-color: rgba(255, 255, 255, 0.8);
      color: #ff5fa2;
      font-weight: bold;
      transition: transform 0.3s, background 0.3s, box-shadow 0.3s;
    }

    .tab-btn:hover, .tab-btn.active {
      transform: scale(1.1);
      background-color: #ffe4ec;
      box-shadow: 0 8px 20px rgba(0,0,0,0.3);
    }

    /* Messages Container */
    .messages-container {
      max-width: 700px;
      margin: 0 auto;
    }

    .message {
      background-color: rgba(255, 255, 255, 0.85);
      color: #ff5fa2;
      padding: 20px 25px;
      margin: 15px 0;
      border-radius: 20px;
      font-size: 1.1em;
      text-align: left;
      box-shadow: 0 8px 20px rgba(0,0,0,0.2);
      cursor: pointer;
      position: relative;
      overflow: hidden;
      transition: transform 0.3s, box-shadow 0.3s, background 0.3s;
    }

    .message:hover {
      transform: scale(1.05);
      box-shadow: 0 12px 25px rgba(0,0,0,0.3);
      background: rgba(255, 255, 255, 0.95);
    }

    .message-content {
      max-height: 0;
      overflow: hidden;
      transition: max-height 0.5s ease, padding 0.5s ease;
    }

    .message.expanded .message-content {
      max-height: 500px;
      padding-top: 10px;
    }

    /* Love Button */
    .love-btn {
      margin: 40px auto;
      padding: 15px 40px;
      font-size: 1.3em;
      border: none;
      border-radius: 30px;
      background-color: white;
      color: #ff5fa2;
      cursor: pointer;
      transition: transform 0.3s, background 0.3s;
      box-shadow: 0 8px 20px rgba(0,0,0,0.2);
    }

    .love-btn:hover {
      transform: scale(1.1) rotate(-5deg);
      background-color: #ffe4ec;
    }

    .footer {
      margin: 60px 0 20px;
      font-size: 1.4em;
      font-weight: bold;
      color: #fff;
      text-shadow: 1px 1px 5px rgba(0,0,0,0.3);
    }

    /* Hearts */
    .heart {
      position: absolute;
      width: 15px;
      height: 15px;
      background-color: #ff5fa2;
      transform: rotate(-45deg);
      animation: float 5s linear infinite;
      pointer-events: none;
      border-radius: 5px;
    }
    .heart:before, .heart:after {
      content: "";
      position: absolute;
      width: 15px;
      height: 15px;
      background-color: #ff5fa2;
      border-radius: 50%;
    }
    .heart:before { top: -7px; left: 0; }
    .heart:after { left: 7px; top: 0; }

    @keyframes float {
      0% {transform: translateY(0) rotate(-45deg); opacity: 1;}
      100% {transform: translateY(-600px) rotate(-45deg); opacity: 0;}
    }

    /* Sparkles */
    .sparkle {
      position: absolute;
      width: 4px;
      height: 4px;
      background-color: #fff;
      border-radius: 50%;
      opacity: 0.8;
      animation: sparkleAnim 2s linear infinite;
      pointer-events: none;
    }
    @keyframes sparkleAnim {
      0% {transform: scale(1) translateY(0); opacity: 0.8;}
      50% {transform: scale(1.5) translateY(-30px); opacity: 0.5;}
      100% {transform: scale(1) translateY(0); opacity: 0;}
    }

    /* Confetti */
    .confetti {
      position: absolute;
      width: 8px;
      height: 8px;
      background-color: #fff;
      opacity: 0.9;
      transform: rotate(45deg);
      pointer-events: none;
      animation: confettiFall 1.5s forwards;
    }

    @keyframes confettiFall {
      0% {transform: translateY(0) rotate(0deg);}
      100% {transform: translateY(300px) rotate(360deg); opacity: 0;}
    }
  </style>
</head>
<body>

  <h1>To: My Ali ❤️</h1>

  <div class="tabs">
    <button class="tab-btn active" data-category="all">All Messages</button>
    <button class="tab-btn" data-category="morning">Morning</button>
    <button class="tab-btn" data-category="sleep">Sleep</button>
    <button class="tab-btn" data-category="emotions">Emotions</button>
    <button class="tab-btn" data-category="fun">Fun & Playful</button>
  </div>

  <div class="messages-container">
    <!-- Example Messages -->
    <div class="message" data-category="morning">
      Open when you wake up
      <div class="message-content">Good morning, love! I hope your day starts with a smile. 💖</div>
    </div>

    <div class="message" data-category="sleep">
      Open when you’re going to sleep
      <div class="message-content">Sweet dreams, babe. 🌙✨</div>
    </div>

    <div class="message" data-category="emotions">
      Open when you feel sad
      <div class="message-content">It’s okay to feel sad sometimes. ❤️</div>
    </div>

    <div class="message" data-category="fun">
      Open when you need a laugh
      <div class="message-content">Remember yung sa Puregold tayo? HAAHHAHA 😂</div>
    </div>

    <!-- Add all your other messages here, keeping data-category for filtering -->
    <!-- ... -->

  </div>

  <button class="love-btn" onclick="alert('I love you mahal ko! 💕')">💖 I Love You 💖</button>

  <div class="footer">From: Dence</div>

  <script>
    // Floating Hearts
    const createHeart = () => {
      const heart = document.createElement('div');
      heart.className = 'heart';
      heart.style.left = Math.random() * window.innerWidth + 'px';
      heart.style.width = heart.style.height = 10 + Math.random() * 15 + 'px';
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 5000);
    };
    setInterval(createHeart, 250);

    // Sparkles
    const createSparkle = () => {
      const sparkle = document.createElement('div');
      sparkle.className = 'sparkle';
      sparkle.style.left = Math.random() * window.innerWidth + 'px';
      sparkle.style.top = Math.random() * window.innerHeight + 'px';
      document.body.appendChild(sparkle);
      setTimeout(() => sparkle.remove(), 2000);
    };
    setInterval(createSparkle, 500);

    // Expand messages with confetti
    const messages = document.querySelectorAll('.message');
    messages.forEach(msg => {
      msg.addEventListener('click', () => {
        msg.classList.toggle('expanded');

        // Confetti effect
        for(let i=0; i<10; i++){
          const confetti = document.createElement('div');
          confetti.className = 'confetti';
          confetti.style.left = (msg.getBoundingClientRect().left + Math.random() * msg.offsetWidth) + 'px';
          confetti.style.top = msg.getBoundingClientRect().top + 'px';
          confetti.style.backgroundColor = `hsl(${Math.random()*360}, 100%, 70%)`;
          document.body.appendChild(confetti);
          setTimeout(() => confetti.remove(), 1500);
        }
      });
    });

    // Tabs filtering
    const tabs = document.querySelectorAll('.tab-btn');
    tabs.forEach(tab => {
      tab.addEventListener('click', () => {
        tabs.forEach(t => t.classList.remove('active'));
        tab.classList.add('active');
        const cat = tab.getAttribute('data-category');
        messages.forEach(msg => {
          msg.style.display = (cat === 'all' || msg.dataset.category === cat) ? 'block' : 'none';
        });
      });
    });
  </script>

</body>
</html>
